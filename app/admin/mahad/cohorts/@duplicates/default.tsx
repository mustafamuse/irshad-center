/**
 * Default fallback for @duplicates slot
 *
 * Rendered when the active route doesn't match this slot's structure.
 * Returns null to show nothing instead of erroring.
 */
export default function DuplicatesDefault() {
  return null
}
