/**
 * Default Modal Slot
 *
 * This renders when no intercepting route is active.
 * Returns null to avoid showing anything when no modal should be displayed.
 */
export default function DefaultModal() {
  return null
}
